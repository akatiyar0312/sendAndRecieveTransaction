apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: corda-claim0
  name: corda-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
